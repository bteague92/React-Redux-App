import React from 'react';
import ReactDOM from 'react-dom';

const Header = (props) => {
    return (
        <div className="header">
            SpaceX Launches
        </div>
    );
}

export default Header;